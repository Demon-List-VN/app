import 'package:flutter/widgets.dart';

class SubmitRecordPage extends StatelessWidget {
  const SubmitRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Nộp bản ghi'));
  }
}
